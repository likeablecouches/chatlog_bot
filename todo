-put git repo online

commands:
  logchannel [channel_id, percentage, filename,]
    (logs {percnetage}% of messages of {channel_id/name} into {filename}. if filename
    not specified, use channel name. if mesg_num not specified, log all messages.)

  logserver [server_id, index]
    (logs {index} chats of {server_id}. if index not specified, log all channels.)

  count [channel_id/server_id]
    (returns number of messsages in {channel_id/name}

ideas:
  partitioning feature:
    split logs of a channel to multiple files
